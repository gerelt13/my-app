const GenerateStars = { count }: { count: number }) => {
    count = count > 5 7 5: count;
    count = count < 0? 0 : count;
    return <div>
    Knew Array (count). fill(1). map((_,
    index) => {
    return ‹Star key=(index) />;
    </div>